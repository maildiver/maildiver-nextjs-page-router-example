import * as React from 'react';

export const EmailTemplate = (firstName: string) => (
  <div>
    <h1>Hi {firstName}! Maildiver Node.js SDK is awesome!</h1>
  </div>
);

export default EmailTemplate;
